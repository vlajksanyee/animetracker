<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Project Checkstyle configuration
  - Based on the default Sun checks, but minimal and tuned for this project.
  - The `PackageName` rule is disabled to allow existing uppercase package segments
    (e.g., Controller, Service, DTOs, Security, Converters).
    If you later refactor packages to all-lowercase, you can re-enable it.
-->
<module name="Checker">
    <!-- Charset to match project defaults -->
    <property name="charset" value="UTF-8"/>

    <!-- File extensions to check -->
    <property name="fileExtensions" value="java"/>

    <!-- Basic file-level checks -->
    <module name="FileTabCharacter"/>
    <module name="NewlineAtEndOfFile"/>

    <module name="TreeWalker">
        <!-- Disable strict package naming for now to avoid failing on uppercase segments -->
        <!-- <module name="PackageName"/> -->

        <!-- Common lightweight checks; you can add more as you like -->
        <module name="UnusedImports"/>
        <module name="AvoidStarImport"/>
        <module name="EmptyBlock">
            <property name="option" value="TEXT"/>
        </module>
        <module name="NeedBraces"/>
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="lineWrappingIndentation" value="8"/>
        </module>
    </module>
</module>
